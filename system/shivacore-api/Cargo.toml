[package]
name = "libshivacore"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Stable ShivaCore userspace ABI: syscalls, capabilities, IPC and handles"

[lints]
workspace = true

[dependencies]
globus-ipc = { path = "../ipc" }
