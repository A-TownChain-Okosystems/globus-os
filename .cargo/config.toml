# ShivaCore must boot on the architectural x86-64 baseline shared by AMD and Intel.
# Keep the kernel artifact free of host-specific AVX/AVX2/SSE4 assumptions.
[target.x86_64-unknown-none]
rustflags = ["-C", "target-cpu=x86-64"]
